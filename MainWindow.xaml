<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CanvasDiagramEditor" 
        x:Class="CanvasDiagramEditor.MainWindow"
        Title="Canvas Diagram Editor" Height="700" Width="900"
        WindowStartupLocation="CenterScreen" WindowState="Normal"
        SnapsToDevicePixels="True" UseLayoutRounding="True">

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Canvas x:Name="DiagramCanvas"
                Width="600" Height="500" 
                Grid.Column="0" Grid.Row="0"
                Background="Black" 
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                PreviewMouseLeftButtonDown="Canvas_PreviewMouseLeftButtonDown"
                MouseMove="Canvas_MouseMove">

            <!--
            <Thumb Canvas.Left="30" Canvas.Top="30" 
                   Template="{StaticResource AndGateControlTemplateKey}" 
                   Style="{StaticResource RootThumbStyleKey}" 
                   DragDelta="RootElement_DragDelta"
                   x:Uid="AndGate|0"/>
        
            <Thumb Canvas.Left="90" Canvas.Top="30" 
                   Template="{StaticResource AndGateControlTemplateKey}" 
                   Style="{StaticResource RootThumbStyleKey}" 
                   DragDelta="RootElement_DragDelta"
                   x:Uid="AndGate|1"/>

            <Thumb Canvas.Left="30" Canvas.Top="90" 
                   Template="{StaticResource AndGateControlTemplateKey}" 
                   Style="{StaticResource RootThumbStyleKey}" 
                   DragDelta="RootElement_DragDelta"
                   x:Uid="AndGate|2"/>

            <Thumb Canvas.Left="90" Canvas.Top="90" 
                   Template="{StaticResource AndGateControlTemplateKey}" 
                   Style="{StaticResource RootThumbStyleKey}" 
                   DragDelta="RootElement_DragDelta"
                   x:Uid="AndGate|3"/>
            -->
            
            <!--<Line Style="{StaticResource LineStyleKey}" X1="60" Y1="45" X2="90" Y2="45" x:Uid="Wire|0"/>-->
            
        </Canvas>

        <Grid Grid.Column="1" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="1" Grid.Row="0">
                <Button x:Name="OpenModel" Content="_Open Model" Margin="2" Click="OpenModel_Click"/>
                <Button x:Name="SaveModel" Content="_Save Model" Margin="2" Click="SaveModel_Click"/>
                <Separator Margin="2"/>
                <Button x:Name="ClearModel" Content="_Clear Model" Margin="2" Click="ClearModel_Click"/>
                <Separator Margin="2"/>
                <Button x:Name="GenerateModel" Content="_Generate Model" Margin="2" Click="GenerateModel_Click"/>
                <Button x:Name="ParseModel" Content="_Parse Model" Margin="2" Click="ParseModel_Click"/>
            </StackPanel>

            <TextBox x:Name="model" Grid.Row="1" 
                     AcceptsReturn="True" AcceptsTab="True" 
                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                     Margin="2" Padding="2"
                     VerticalScrollBarVisibility="Auto">
            </TextBox>

        </Grid>
    
    </Grid>
</Window>
