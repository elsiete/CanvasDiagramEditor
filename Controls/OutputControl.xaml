<UserControl x:Class="CanvasDiagramEditor.Controls.OutputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="180">
    
    <Canvas Width="180" Height="30" Style="{StaticResource ControlCanvasStyleKey}">

        <Canvas x:Name="RootElement" Width="180" Height="30" Style="{StaticResource RootCanvasStyleKey}">
            <Path Data="M 0,0 L 180,0 M 0,30 L 180,30 M 0,0 L 0,30 M 180,0 L 180,30" Style="{StaticResource LogicPathStyleKey}"/>
            <Grid Width="180" Height="30">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Output" Style="{StaticResource LogicTextStyleKey}"/>
            </Grid>
        </Canvas>

        <Thumb x:Name="LeftPin" Canvas.Left="0" Canvas.Top="15" 
               Template="{StaticResource PinThumbControlTemplateKey}"
               Style="{StaticResource PinhumbStyleKey}"/>

    </Canvas>
    
</UserControl>
