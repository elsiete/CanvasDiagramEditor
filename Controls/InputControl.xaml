<UserControl x:Class="CanvasDiagramEditor.Controls.InputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="285">
    
    <Canvas Width="285" Height="30" Style="{StaticResource ControlCanvasStyleKey}">

        <Canvas x:Name="RootElement" Width="285" Height="30" Style="{StaticResource RootCanvasStyleKey}">
            <Path Data="M 0,0 L 285,0 M 285,30 L 0,30 M 0,30  L 0,0 M 210,0 L 210,30 M 285,30 L 285,0" Style="{StaticResource LogicPathStyleKey}"/>
            <Grid Width="285" Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="210"/>
                    <ColumnDefinition Width="75"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Input" Style="{StaticResource LogicTextStyleKey}"/>
            </Grid>
        </Canvas>

        <Thumb x:Name="RightPin" Canvas.Left="285" Canvas.Top="15" 
               Template="{StaticResource PinThumbControlTemplateKey}"
               Style="{StaticResource PinhumbStyleKey}"/>

    </Canvas>
    
</UserControl>
